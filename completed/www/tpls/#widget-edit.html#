<form>

	<div>
		<label>Name: <input type="text" ng-model="$ctrl.widget.name"></label>
	</div>
	<div>
		<label>Description: <textarea ng-model="$ctrl.widget.description" rows="5" cols="60"></textarea></label>
	</div>
	<div>
		<label>Color:
			<select ng-model="$ctrl.widget.color" ng-options="color.value as color.label for color in $ctrl.colors">
				<option value="">Select One</option>
			</select>
		</label>
	</div>
	<div>
		<label>Size:
			<select ng-model="$ctrl.widget.size" ng-options="size.value as size.label for size in $ctrl.sizes">
				<option value="">Select One</option>
			</select>
		</label>
	</div>
	<div>
		<label>Quantity: <input type="number" ng-model="$ctrl.widget.quantity"></label>
	</div>

Save Widget
	<button class="btn btn-danger" ng-if="$ctrl.widget.id" ng-click="$ctrl.deleteWidget($ctrl.widget.id)">Delete Widget</button>
	<button class="btn btn-default" ng-click="$ctrl.returnToList()">Return to List</button>

</form>
